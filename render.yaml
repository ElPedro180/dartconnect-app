services:
  - type: web
    name: dartconnect-web
    env: python
    buildCommand: |
      pip install -r requirements.txt
      playwright install
    startCommand: gunicorn app:app
    plan: free